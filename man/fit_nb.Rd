% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fit-nb.R
\name{fit_nb}
\alias{fit_nb}
\alias{fit_nb.default}
\alias{fit_nb.formula}
\alias{fit_nb.tr_data}
\alias{print.libml_nb}
\alias{predict.libml_nb}
\alias{plot.libml_nb}
\alias{plot.naiveBayes}
\title{Robustly Fit Naive Bayes Classifier}
\usage{
fit_nb(x, ...)

\method{fit_nb}{default}(x, y, mad = FALSE, laplace = 0, keep.data = FALSE, ...)

\method{fit_nb}{formula}(formula, data, ...)

\method{fit_nb}{tr_data}(x, ...)

\method{print}{libml_nb}(x, ...)

\method{predict}{libml_nb}(
  object,
  newdata,
  type = c("class", "posterior", "raw"),
  threshold = NULL,
  ...
)

\method{plot}{libml_nb}(
  x,
  data,
  features,
  plot_type = c("pdf", "cdf", "log_odds"),
  x_lab = "value",
  id,
  ...
)

\method{plot}{naiveBayes}(
  x,
  data,
  features,
  plot_type = c("pdf", "cdf", "log_odds"),
  x_lab = "value",
  id,
  ...
)
}
\arguments{
\item{x}{A numeric matrix, a \code{tr_data} class objects, or a data frame
of predictors. If called from an S3 generic method (e.g.
\code{\link[=plot.libml_nb]{plot.libml_nb()}}) or \code{\link[=print.libml_nb]{print.libml_nb()}}), either a
\code{libml_nb} or \code{naiveBayes} object.}

\item{...}{Additional arguments passed to the default \code{\link[=fit_nb]{fit_nb()}}
default method. Currently not used in the \code{predict} or \code{print} S3 methods,
but is used in the S3 plot method, arguments passed to
\code{\link[SomaPlotr:plotCDF]{SomaPlotr::plotCDFlist()}} or \code{\link[SomaPlotr:plotPDF]{SomaPlotr::plotPDFlist()}}.}

\item{y}{\code{factor(n)}. If not passing a formula, a factor with
true class names for each sample (row) in \code{x}.}

\item{mad}{\code{logical(1)}. Should non-parametric approximations be
applied during the parameter estimation procedure. See \code{Details}.}

\item{laplace}{\code{double(1)}. Positive controlling Laplace smoothing.
The default (\code{0}) disables Laplace smoothing.}

\item{keep.data}{Logical. Should the training data used to fit the model be
included in the model object? When building thousands of models, this can
become a memory issue and thus the default is \code{FALSE}.}

\item{formula}{A model formula of the form: \code{class ~ x1 + x2 + ...}
(no interactions).}

\item{data}{A data frame of predictors (categorical and/or numeric), i.e.
the ADAT used to train the model.}

\item{object}{A \code{libml_nb} model object.}

\item{newdata}{A \code{data.frame} with new predictors, containing at least
the model covariates (possibly more columns than the training data).
Note that the column names of \code{newdata} are matched against the
training data.}

\item{type}{If \code{"class"} (default), the class name with maximal
posterior probability is returned for each sample, otherwise the
conditional \emph{a-posterior} probabilities for each class are returned.
Additionally, if called from within the S3 plot method, a character
string determining the plot type, currently either CDF or PDF (default).
Argument can be shortened and is matched.}

\item{threshold}{\code{numeric(1)}. Indicating the minimum probability
a prediction can take.}

\item{features}{An optional feature specifying which subset of model features
to plot. If missing, all features are plotted.}

\item{plot_type}{\code{character(1)}. A string determining the plot type, currently
either a probability density function (PDF, default), CDF, or log-odds
plots. Matched via \code{\link[=match.arg]{match.arg()}}.}

\item{x_lab}{\code{character(1)}. Optional label for the x-axis.}

\item{id}{\code{integer(n)} or \code{character(n)}. Optional identifier of
a specific sample to plot on top of either PDFs or CDFs.
Either an index of the sample row in the \code{data}, or its \code{rowname}.}
}
\value{
\code{libml_nb}: A naive Bayes model with robustly fit parameters.

\code{predict.libml_nb}: depending on the \code{type} argument,
either the predicted class name or the posterior probability
of a robustly estimated naive Bayes model.

\code{plot.libml_nb}: A plot, either a list of
PDFs/CDFs, or a log-odds plot.
}
\description{
Computes the conditional \emph{a}-posterior probabilities of a
categorical class variable given independent predictor
variables using the Bayes rule.
Parameter estimates are robustly calculated using approximations
of the error function for a Gaussian density, see \code{\link[=fit_gauss]{fit_gauss()}}.
}
\details{
When \code{mad = TRUE} (median absolute deviation), non-parametric
calculation of Bayes' parameters are estimated,
namely, \code{mu = median(x)} and \code{sd = IQR(x) / 1.349}.
That is, \code{fit_gauss(..., mad = TRUE)}.
}
\section{Methods (by class)}{
\itemize{
\item \code{fit_nb(default)}: S3 \code{default} method for fit_nb.

\item \code{fit_nb(formula)}: S3 \code{formula} method for fit_nb.

\item \code{fit_nb(tr_data)}: S3 \code{tr_data} method for fit_nb.

}}
\section{Functions}{
\itemize{
\item \code{print(libml_nb)}: S3 print method for \code{libml_nb}.

\item \code{predict(libml_nb)}: S3 predict method for \code{libml_nb}.

\item \code{plot(libml_nb)}: S3 plot method for \code{libml_nb}.

\item \code{plot(naiveBayes)}: Plot a \code{naiveBayes} (\code{e1071}) model object.

}}
\examples{
head(tr_iris)
# standard naive Bayes
m1 <- e1071::naiveBayes(Species ~ ., data = tr_iris) # non-robust params
m2 <- fit_nb(Species ~ ., data = tr_iris)  # formula syntax
m3 <- fit_nb(tr_iris)                      # tr_data syntax
m4 <- data.frame(tr_iris[, -5L]) |>
  fit_nb(y = tr_iris$Species)              # default syntax

# not same
identical(sapply(m1$tables, as.numeric), sapply(m2$tables, as.numeric))

# same
identical(sapply(m2$tables, as.numeric), sapply(m3$tables, as.numeric))

# Predictions
table(predict(m1, iris), iris$Species) # benchmark
table(predict(m2, iris), iris$Species) # approx same for Gaussian data; no outliers

# Plotting
plot(m2, tr_iris)
plot(m2, tr_iris, id = 20)      # sample 20 is definitely "virginica"
plot(m1, tr_iris, plot_type = "cdf")  # plot type CDF
plot(m2, tr_iris, features = "Sepal.Length", id = 70)  # 1 feature
plot(m1, tr_iris, plot_type = "cdf", lty = "longdash") # pass-through of lty
}
\references{
This function was \emph{heavily} influenced by \code{\link[e1071:naiveBayes]{e1071::naiveBayes()}}
See David Meyer <email: David.Meyer@R-project.org>.
}
\seealso{
\code{\link[=fit_gauss]{fit_gauss()}}

\code{\link[=plot_log_odds]{plot_log_odds()}}, \code{\link[SomaPlotr:plotPDF]{SomaPlotr::plotPDFlist()}}, \code{\link[SomaPlotr:plotCDF]{SomaPlotr::plotCDFlist()}}

The `fit*()` family: 
\code{\link{fit_gbm}()},
\code{\link{fit_kknn}()},
\code{\link{fit_logistic}()}
}
\author{
Stu Field
}
\concept{fit}
