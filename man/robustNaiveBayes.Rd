% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/robustNaiveBayes.R
\name{robustNaiveBayes}
\alias{robustNaiveBayes}
\alias{robustNaiveBayes.default}
\alias{robustNaiveBayes.formula}
\alias{print.robustNaiveBayes}
\alias{predict.robustNaiveBayes}
\alias{plot.robustNaiveBayes}
\alias{plot.naiveBayes}
\title{Robustly Fit Naive Bayes Classifier}
\usage{
robustNaiveBayes(x, ...)

\method{robustNaiveBayes}{default}(x, y, mad = FALSE, laplace = 0, keep.data = FALSE, ...)

\method{robustNaiveBayes}{formula}(formula, data, ...)

\method{print}{robustNaiveBayes}(x, ...)

\method{predict}{robustNaiveBayes}(
  object,
  newdata,
  type = c("class", "posterior", "raw"),
  threshold = 1e-06,
  min.prob = NULL,
  ...
)

\method{plot}{robustNaiveBayes}(
  x,
  data,
  features,
  plot.type = c("pdf", "cdf", "log.odds"),
  x.lab = bquote(italic(log)[10] ~ (RFU)),
  sampleId,
  ...
)

\method{plot}{naiveBayes}(
  x,
  data,
  features,
  plot.type = c("pdf", "cdf", "log.odds"),
  x.lab = bquote(italic(log)[10] ~ (RFU)),
  sampleId,
  ...
)
}
\arguments{
\item{x}{A numeric matrix, or a data frame of categorical and/or numeric
variables. If called from an S3 generic method (e.g.
\code{\link[=plot.robustNaiveBayes]{plot.robustNaiveBayes()}}) or \code{\link[=print.robustNaiveBayes]{print.robustNaiveBayes()}}), either a
\code{robustNaiveBayes} or \code{naiveBayes} class object.}

\item{...}{Additional arguments passed to the default \code{\link[=robustNaiveBayes]{robustNaiveBayes()}}
default method. Currently not used in the \code{predict} or \code{print} S3 methods,
but is used in the S3 plot method, arguments passed to
\code{\link[SomaPlotr:plotCDF]{SomaPlotr::plotCDFlist()}} or \code{\link[SomaPlotr:plotPDF]{SomaPlotr::plotPDFlist()}}.}

\item{y}{A vector indicating the true classes for each sample. Ideally a
factor class object with appropriate levels.}

\item{mad}{Logical. Should non-parametric approximations be applied during
the parameter estimation procedure. See \code{Details} section.}

\item{laplace}{positive double controlling Laplace smoothing. The default
(\code{0}) disables Laplace smoothing.}

\item{keep.data}{Logical. Should the training data used to fit the model be
included in the model object? When building thousands of models, this can
become a memory issue and thus the default is \code{FALSE}.}

\item{formula}{A model formula of the form: \code{class ~ x1 + x2 + ...}
(no interactions).}

\item{data}{A data frame of predictors (categorical and/or numeric), i.e.
the ADAT used to train the model.}

\item{object}{A model object of class \code{robustNaiveBayes}.}

\item{newdata}{A \code{data.frame} with new predictors, containing at least
the model covariates (but possibly more columns than the training data).
Note that the column names of \code{newdata} are matched against the
training data ones.}

\item{type}{If \code{"class"} (default), the class name with maximal
posterior probability is returned for each sample, otherwise the
conditional \emph{a-posterior} probabilities for each class are returned.
Additionally, if called from within the S3 plot method, a character
string determining the plot type, currently either CDF or PDF (default).
Argument can be shortened and is matched.}

\item{threshold}{Value below which should be replaced. See \code{min.prob}.}

\item{min.prob}{Value indicating the minimum probability a prediction
can take. See \code{threshold} argument.}

\item{features}{An optional feature specifying which subset of model features
to plot. If missing, all features are plotted.}

\item{plot.type}{Character. A string determining the plot type, currently
either a probability density function (PDF, default), CDF, or log-odds
plots. Arguments can be shortened and is matched via \code{\link[=match.arg]{match.arg()}}.}

\item{x.lab}{Character. Optional label for the x-axis.}

\item{sampleId}{An optional identifier of a specific sample to plot on top of
either PDFs or CDFs. This may be either a numeric index of the sample row
in the \code{data}, or its row name identifier. Can be of \code{length > 1}.}
}
\value{
\code{robustNaiveBayes}: A naive Bayes model with robustly fit
coefficients.

\code{predict.robustNaiveBayes}: Depending on the \code{type} argument,
the posterior probability of a robustly estimated naive Bayes model.

\code{plot.robustNaiveBayes}, \code{plot.naiveBayes}: A plot, either a list of
PDFs/CDFs, or a log-odds plot.
}
\description{
Computes the conditional \emph{a}-posterior probabilities of a
categorical class variable given independent predictor
variables using the Bayes rule.
Parameter estimates are robustly calculated using approximations
of the error function for a Gaussian density, see \code{\link[=calcRobustGaussFit]{calcRobustGaussFit()}}.
}
\details{
When \code{mad = TRUE} (median absolute deviation), non-parametric
calculation of Bayes' parameters are estimated,
namely, \code{mu = median(x)} and \code{sd = IQR(x) / 1.349}.
That is, \code{calcRobustGaussFit(..., mad = TRUE)}.
}
\section{Methods (by class)}{
\itemize{
\item \code{robustNaiveBayes(default)}: S3 default method for robustNaiveBayes.

\item \code{robustNaiveBayes(formula)}: S3 formula method for robustNaiveBayes.

}}
\section{Methods (by generic)}{
\itemize{
\item \code{print(robustNaiveBayes)}: S3 print method for robustNaiveBayes.

\item \code{predict(robustNaiveBayes)}: S3 predict method for robustNaiveBayes.

\item \code{plot(robustNaiveBayes)}: S3 plot method for \code{robustNaiveBayes}.

}}
\section{Functions}{
\itemize{
\item \code{plot(naiveBayes)}: Plot a \code{naiveBayes} model object.

}}
\examples{
head(iris)
# standard naiveBayes
m1 <- e1071::naiveBayes(Species ~ ., data = iris)  # non-robust
m2 <- robustNaiveBayes(iris[, -5], iris$Species)   # robust fitting
m3 <- robustNaiveBayes(Species ~ ., data = iris)   # with formula syntax
identical(sapply(m1$tables, as.numeric), sapply(m2$tables, as.numeric)) # not same
identical(sapply(m2$tables, as.numeric), sapply(m3$tables, as.numeric)) # same

# Predictions
table(predict(m1, iris), iris$Species) # benchmark
table(predict(m2, iris), iris$Species) # approx same for Gaussian data; no outliers

# Plotting
iris <- create_train(iris, group.var = Species)
plot(m2, iris)
plot(m2, iris, sampleId = 50)      # sample 50 is definitely "setosa"
plot(m1, iris, plot.type = "cdf")  # plot type CDF
plot(m2, iris, features = "Sepal.Length", sampleId = 70)  # 1 feature
plot(m1, iris, plot.type = "cdf", lty = "longdash")   # pass through of linetype
}
\references{
This function was \emph{heavily} influenced by \code{\link[e1071:naiveBayes]{e1071::naiveBayes()}}
See David Meyer <email: David.Meyer@R-project.org>.
}
\seealso{
\code{\link[=calcRobustGaussFit]{calcRobustGaussFit()}}

\code{\link[=plotLogOdds]{plotLogOdds()}}, \code{\link[SomaPlotr:plotPDF]{SomaPlotr::plotPDFlist()}}, \code{\link[SomaPlotr:plotCDF]{SomaPlotr::plotCDFlist()}}
}
\author{
Stu Field
}
